spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/sepa_payments
      auto-index-creation: true
  application:
    name: sepa-payment-backend

server:
  port: 8080

app:
  transaction:
    ttl-minutes: 5
    ref-prefix: "TXN"
  qr:
    base-url: http://localhost:5174/pay
  bank-mock:
    base-url: http://localhost:8081
